# Installed as /etc/anigadgets/api.env; future provisioning preserves edits.
# Exact browser origin, without a trailing slash or /api.
FRONTEND_URL=https://anigadgetsbd.app
# Leave disabled until one trusted proxy overwrites untrusted forwarded IPs.
TRUST_PROXY=0